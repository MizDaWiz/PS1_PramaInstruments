#include <Adafruit_GFX.h> // Core graphics library
#include "SWIFT.h" // Hardware-specific library
//#include <TouchScreen.h>
//// The control pins for the LCD can be assigned to any digital or
//// analog pins...but we'll use the analog pins as this allows us to
//// double up the pins with the touch screen (see the TFT paint example).
//#define LCD_CS A3 // Chip Select goes to Analog 3
//#define LCD CD A2 // Command/Data goes to Analog 2
//#define LCD WR A1 // LCD Write goes to Analog 1
//#define LCD RD AO // LCD Read goes to Analog 0
//#define LCD RESET A4 // Can alternately just connect to Arduino's reset pin
//// When using the BREAKOUT BOARD only, use these 8 data lines to the LCD:
//// For the Arduino Uno, Duemilanove, Diecimila, etc.:
//11
//DO connects to digital pin 8
//(Notice these are
//11
//D1 connects to digital pin 9
//NOT in order!)
//11
//D2 connects to digital pin 2
//11
//D3 connects to digital pin 3
//11
//D4 connects to digital pin 4
//11
//DS connects to digital pin 5
//11
//D6 connects to digital pin 6
//11 D7 connects to digital pin 7
// For the Arduino Mega, use digital pins 22 through 29
// (on the 2-row header at the end of the board).
// Assign human-readable names to some common 16-bit color values:
// Assign human-readable names to some common 16-bit color values:
#define BLACK 0x0000
#define BLUE 0x001F
#define RED OxF800
#define GREEN 0x07E0
#define CYAN 0x07FF
#define MAGENTA OXF81F
#define YELLOW OXFFEO
#define WHITE OxFFFF
//SWIFT tft:
//#define YP A1
//#define XM A2
//#define YM 7
// must be an analog pin, use "An" notation!
// must be an analog pin, use "An" notation!
// can be a digital pin


//
//#define YP A1 // must be an analog pin, use "An" notation!
//#define XM A2 // must be an analog pin, use "An" notation!
//#define YM 7 // can be a digital pin
//#define XP 6
//TouchScreen ts TouchScreen (XP, YP, XM, YM, 300):
#define MINPRESSURE 10
#define MAXPRESSURE 1000
#define DEBUG true


String Host_name "Rnd";
String password = "12345678";
//WiFi SSID Name and Password ******



void RESET();
void Buttons();
String Gold ABAD():
String TEMP ABAD(): I
void setup (void) {
Serial.begin(115200);
Seriall.begin(115200);

t
void loop(void) {
  if (DEBUG)
Serial.println("TEMP");

if (x < 320 X 226 Y <175 Y> 140)
(

if (DEBUG)
Serial.println("Views");

}
